# Delivered notes, delivery state, and the local mirror all hold real
# conversation content. They must never reach a git remote.
out/
state/
*.db
*.db-wal
*.db-shm
.note-*
.state-*

__pycache__/
*.pyc
.pytest_cache/
.venv/
